{
  "private": true,
  "packageManager": "pnpm@8",
  "author": "Thijs Koerselman",
  "license": "MIT",
  "description": "A monorepo setup with shared ESM packages deploying to multiple platforms including Firebase",
  "repository": {
    "type": "git",
    "url": "https://github.com/0x80/mono-turbo-next-fire"
  },
  "scripts": {
    "type-check": "turbo run type-check",
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "turbo": "^1.10.14",
    "typescript": "^5.2.2",
    "vercel": "^32.2.4",
    "vitest": "^0.34.4"
  }
}
